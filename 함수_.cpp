#include "pch.h"
#include <iostream>
using namespace std;

int star(int count, string name); //함수 정의 _ 소괄호 안에는 입력값
{
	for (int i = 0; i < 10; i++)
	{
		cout << shape;
	}
	cout << name << endl;
	return 1;
}
int main()
{
	//star(); //함수를 사용, call
	//star(); //함수를 사용, call
	//star(); //함수를 사용, call
	//star(); //함수를 사용, call
	//star(); //함수를 사용, call
	int result = star(50,"☏");
	if (result == 1) {
		cout << "별을 잘 찍었군요." << endl;
		}
	else {
		cout << "별을 잘못 찍었군요." << endl;
	}
}
//1. hi라는 함수"안녕하세요" 출력하도록 정의 => 10번 출력
//2. add(200, 100) => 합 300입니다.
//3. tv(10) => 길동이는 10시간동안 텔레비전을 봅니다.

//매개변수 = parameter